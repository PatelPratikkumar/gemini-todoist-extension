name = "labels:list"
description = "List all labels"
usage = "/todo:labels"

prompt = """
List all Todoist labels.

Call get_labels and format showing:
- Label name
- Color
- Favorite status

User input: {{args}}
"""
