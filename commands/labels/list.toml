name = "todo:labels"
description = "List all labels"
usage = "/todo:labels"

[[steps]]
type = "system"
text = """
The user wants to see all their labels.

Steps:
1. Call get_labels
2. Format output showing:
   - Label name
   - Color
   - Whether it's a favorite

Example output:
🏷️ Labels:
├── ⭐ urgent (red)
├── work (blue)
├── personal (green)
└── errands (orange)
"""
