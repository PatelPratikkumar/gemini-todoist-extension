name = "todo:projects"
description = "List all Todoist projects"
usage = "/todo:projects"

prompt = """
List all Todoist projects.

Call get_projects and format as a structured list showing:
- Project name
- Color indicator
- Favorite status (⭐)
- View style (list/board)

Group favorites first, then alphabetically.

User input: {{args}}
"""
