name = "todo:project-delete"
description = "Delete a project and all its tasks"
usage = "/todo:project-delete [project name]"

prompt = """
Delete a project. EXTREME DANGER: Deletes project AND ALL TASKS!

1. Find project with get_projects
2. Count tasks in project
3. Show warning with task count
4. Ask user to type exact project name to confirm
5. Only proceed if confirmed exactly
6. Call delete_project

NEVER skip confirmation!

User input: {{args}}
"""
