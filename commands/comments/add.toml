name = "comments:add"
description = "Add a comment to a task"
usage = "/todo:comment-add [task] --message TEXT"

prompt = """
Add a comment to a task. Parse input for:
- Task name/identifier
- --message or quoted text: comment content

Find task, call create_comment with task_id and content.

User input: {{args}}
"""
