name = "tasks:update"
description = "Update an existing Todoist task"
usage = "/todo:update [task] --content TEXT --due DATE --priority N"

prompt = """
Update an existing task. Parse input for:
- Task name/identifier
- --content: new task content
- --due: new due date
- --priority: new priority (1-4)
- --label: add/change labels

Find task with get_tasks, show current details, apply updates with update_task.

User input: {{args}}
"""
