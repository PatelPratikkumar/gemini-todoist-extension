name = "todo:delete"
description = "Delete a Todoist task permanently"
usage = "/todo:delete [task name]"

[[steps]]
type = "system"
text = """
The user wants to delete a task.

🚨 DANGER: This is PERMANENT and cannot be undone!

Steps:
1. Use get_tasks to find the matching task
2. If multiple matches, list them and ask user to specify
3. Show the exact task: "Task: [name] in [project]"
4. Warn: "⚠️ This will PERMANENTLY delete this task. This cannot be undone."
5. Ask user to type "DELETE" to confirm
6. Only proceed if user types exactly "DELETE"
7. Use delete_task with the task ID
8. Confirm deletion

NEVER skip the confirmation step!
"""
