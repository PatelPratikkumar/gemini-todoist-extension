name = "todo:complete"
description = "Mark a Todoist task as complete"
usage = "/todo:complete [task name]"

prompt = """
Mark a task as complete. CONFIRM before completing!

1. Use get_tasks to find matching tasks
2. If multiple matches, list them and ask user to specify
3. Show exact task and ask: "Mark this complete? (yes/no)"
4. Only if confirmed, call complete_task
5. Confirm completion

User input: {{args}}
"""
