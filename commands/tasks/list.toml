name = "todo:list"
description = "List Todoist tasks with optional filters"
usage = "/todo:list [--project NAME] [--priority 1-4] [--filter QUERY]"

prompt = """
List Todoist tasks. Parse filters from input:
- --project NAME: filter by project
- --priority N: filter by priority (1-4)
- --label NAME: filter by label
- --filter QUERY: Todoist filter syntax (today, overdue, p1)

If no filters, default to today's tasks.
Call get_tasks with filters and format as a clear list.

User input: {{args}}
"""
